# offline-ai-chat-app
A standalone AI chat application built using Python that works completely offline using a lightweight local LLM.

Copy .env.example → .env and fill values

Model downloads automatically on first launch

Run command: python main.py

Build command: uv run python -m PyInstaller rafigpt.spec --clean
